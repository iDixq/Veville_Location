{% extends 'base.html.twig' %}
{% block title %} {{ parent()}} Vehicule | Update{% endblock %}
{% block body %}

<div class="row justify-content-end">
    <a class="btn btn-info p-2 col-md-3" href="{{ path('app_vehicule') }}">Retour</a>
</div>

<div class="col_md-4 mx-auto">

{{ form_start(formVehicule) }}

    <div class="col-md-10 mx-auto">

{{ form_row(formVehicule) }}
    </div>

    {{ form_end(formVehicule) }}

</div>

{% endblock %}