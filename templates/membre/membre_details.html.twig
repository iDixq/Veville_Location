{% extends 'base.html.twig' %}

{% block title %} {{ parent()}} Membres | Details{% endblock %}

{% block body %}

<div class="container">

 <table class="table">
  <thead>
    <tr>

      <th scope="col">#id_membre</th>
      <th scope="col">Pseudo</th>
      <th scope="col">Nom</th>
      <th scope="col">Prénom</th>
      <th scope="col">Email</th>
      <th scope="col">Civilite</th>
      <th scope="col">Statut</th>
      <th scope="col">Date_enregistrement</th>
      <th scope="col">Actions</th>

    </tr>
  </thead>
  <tbody>
  
            <tr>
                <td>{{ membre.id }}</td>
                <td>{{ membre.pseudo }}</td>
                <td>{{ membre.nom }}</td>
                <td>{{ membre.prenom }}</td>
                <td>{{ membre.email }}</td>
                <td>{{ membre.civilite }}</td>
                <td>{{ membre.statut }}</td>
                {# <td>{{ membre.date_enregistrement}}</td> #}
                
                
                {# {{ path("category_modifier", {"id" : membre.id})}} #}


            <td>
                
            </td>

            <td>
                <a href="{{ path("membre_update", {"id" : membre.id})}}" >
                    <img src="{{ asset("images/update.png")}}" alt="icône de modification" style="width:2rem">
                </a>

                <a href="{{ path("membre_delete", {"id" : membre.id})}}" >
                    <img src="{{ asset("images/delete.png")}}" alt="icône de suppression" style="width:2rem">
                </a>

                <a href="{{ path("membre_details", {"id" : membre.id})}}" >
                    <img src="{{ asset("images/details.png")}}" alt="icône de suppression" style="width:2rem">
                </a>
            </td> 

            </tr>

           
   
    
  </tbody>
</table>
</div>


{% endblock %}