{% extends 'base.html.twig' %}

{% block title %}| Commandes |{% endblock %}

{% block body %}

<div class="container">
{% if commandes %}
    <table class="table">
  <thead>
    <tr>

      <th scope="col">#id_commande</th>
      <th scope="col">#id_membre</th>
      <th scope="col">#id_vehicule</th>
      <th scope="col">#id_agence</th>
      <th scope="col">date_heure_depart</th>
      <th scope="col">date_heure_fin</th>
      <th scope="col">prix_total</th>
      <th scope="col">Date_enregistrement</th>
      <th scope="col">Actions</th>

    </tr>
  </thead>
  <tbody>
  {% for commande in commandes %}
            <tr>
                <td>{{ commande.id }}</td>
                <td>{{ commande.pseudo }}</td>
                <td>{{ commande.nom }}</td>
                <td>{{ commande.prenom }}</td>
                <td>{{ commande.email }}</td>
                <td>{{ commande.civilite }}</td>
                <td>{{ commande.statut }}</td>
                <td>{{ commande.dateEnregistrement|date("Y-m-d H:i:s")}}</td>
            
                {# {{ path("category_modifier", {"id" : commande.id})}} #}

            <tr>
                
            </td>

            <td>
                <a href="{{ path("commande_update", {"id" : commande.id})}}" >
                    <img src="{{ asset("images/update.png")}}" alt="icône de modification" style="width:2rem">
                </a>

                <a href="{{ path("commande_delete", {"id" : commande.id})}}" >
                    <img src="{{ asset("images/delete.png")}}" alt="icône de suppression" style="width:2rem">
                </a>

                <a href="{{ path("membre_details", {"id" : membre.id})}}" >
                    <img src="{{ asset("images/details.png")}}" alt="icône de suppression" style="width:2rem">
                </a>
            </td> 

            </tr>

            {% endfor %}
   
    
  </tbody>
</table>

{% endif %}

 {{ form_start(formMembre)}}

 <div class="col-md-10 mx-auto">
        {{ form_row(formMembre.pseudo) }}
    </div>
<div class="col-md-10 mx-auto">
        {{ form_row(formMembre.mdp) }}
    </div>

    <div class="col-md-10 mx-auto">
        {{ form_row(formMembre.nom) }}
    </div>

    <div class="col-md-10 mx-auto">
        {{ form_row(formMembre.prenom) }}
    </div>

    <div class="col-md-10 mx-auto">
        {{ form_row(formMembre.email) }}
    </div>

    <div class="col-md-10 mx-auto">
        {{ form_row(formMembre.civilite) }}
    </div>
    <div class="col-md-10 mx-auto">
        {{ form_row(formMembre.statut) }}
    </div>


    <div class="row justify-content-center">
        <input type="submit" class="btn btn-success mt-3 col-md-6" value="Ajouter">
    </div>
</div>
{% endblock %}
